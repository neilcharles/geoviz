# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages
before_install:
  - sudo apt-get -y install libgdal-dev libudunits2-dev libx11-dev mesa-common-dev libglu1-mesa-dev
